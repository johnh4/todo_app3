<!DOCTYPE html>
<html>
<head>
  <title>TodoApp3</title>
  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
	
	<ul>
		<% if current_user %>
			<div id="logged_as">Logged in as <%= current_user.email %></div>
			<li class="navbar_item"><%= link_to "Sign out", signout_path %></li>
		<% else %>
			<li class="navbar_item"><%= link_to "Sign in", signin_path %></li>
			<li class="navbar_item"><%= link_to "Sign up", signup_path %></li>
		<% end %>
	</ul>

	<%= flash.each do |name, msg| %>
		<% content_tag :div, msg, id: "flash_#{name}" %>
	<% end %>

	<%= yield %>

</body>
</html>
